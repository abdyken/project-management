# false sends requests to the API. true keeps the in-browser mocks.
VITE_USE_MOCKS=false

# Leave empty in local dev: the Vite server proxies /api to the backend.
# Set this to the deployed API origin for a production build.
VITE_API_BASE_URL=
